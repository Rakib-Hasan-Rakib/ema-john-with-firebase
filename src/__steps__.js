/**
 * 1. create a firebase project
 * 2. save firebase config and export app
 * 3. Build> Authentication> enable sign in methods
 * 4. create sign in & sign up components
 */

/**
 * 1. create a context provider file
 * 2. create a context and set provider
 * 3. set the children props
 * 4. set context value
 * 5. set the provider
 */


/**
 * Hosting 
 * one time per PC
 * 1. npm i -g firebase-tools (g for global)
 * 2. firebase login
 * 3. for each project firebase init
 * 4. procced
 * 5. firebase Hosting .keyboard up down use space bar to select
 * 6. existing project 
 * 7. select project
 * 8. which project as public directory : dist
 * 9. single page application : yes
 * 10. github : no
 * 
 * 
 * for every time deploy
 * 1.npm run build
 * 2. firebase deploy
 */